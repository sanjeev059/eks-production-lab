apiVersion: v1
kind: Service

metadata:
  name: auth-service
  namespace: production
  labels:
    app: auth-service
    tier: backend

spec:
  type: ClusterIP

  selector:
    app: auth-service
    tier: backend

  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP